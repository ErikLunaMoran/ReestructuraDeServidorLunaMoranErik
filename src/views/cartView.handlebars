<html>
  <head>
    <title>Detalle del Carrito</title>
  </head>
  <body>
    <h1>Carrito de Compras</h1>
    <h2>Productos en el carrito:</h2>
    <ul>
      {{#each cart.products}}
        <li>
          <strong>Producto: {{this.product.title}}</strong><br />
          Descripción:
          {{this.product.description}}<br />
          Cantidad:
          {{this.quantity}}<br />
          Precio por unidad: ${{this.product.price}}<br />
          Precio total: ${{this.product.price this.quantity}}<br />
        </li>
      {{/each}}
    </ul>
    <p>Total del carrito: ${{cart.totalPrice}}</p>
  </body>
</html>